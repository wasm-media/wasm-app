# الحزمة 1 — من التجريبي إلى الشغل الحي

**الحالة:** **معتمدة** — عيسى: «موافق على الحزمة 1» 25/09/2026 20:38. قيد التنفيذ.
**الهدف القابل للقياس:**
- عيسى وأحمد يفتحان شغلة حقيقية على التطبيق المنشور، ويحرّكانها، من جهازيهما.
- الموعد: 01/10/2026 (البند P16).
- كل تغيير يمرّ من الأقفال: الفحص الآلي، ثم ضغطة عيسى.

## طريقة العمل في الحزم (D23)
- **موافقة واحدة في البداية على الحزمة كلها** تشمل كل الخطوات المسمّاة أدناه، ومنها الأعمال في المتصفح المذكورة بالاسم.
- **لقاء واحد في النهاية للتسليم:** ضغطات Approve، ثم التجربة.
- **في المنتصف لا أسئلة.** القرارات التقنية تُتخذ بالافتراضات المكتوبة هنا، وتُسجَّل في DECISIONS.
- **المراجعة المستقلة لا تُحذف.** كل قطعة تمرّ على code-reviewer بجولتين، وحارس المؤجلات يفحص قبل التسليم. هذه تجري داخل الحزمة، ولا تحتاج كلمة عيسى إلا عند شروط التوقف.
- **جواب §3.3 معتمد مسبقًا:** إن رُفضت قطعة في جولة المراجعة الثانية، يُفصل الإصلاح في حزمة صغيرة ضمن الحزمة نفسها وتكمل، كما حصل في «الخروج الآمن». لا توقف إلا إن كان العيب أمنيًا ولا يُصلح داخل النطاق.

## ما في الحزمة (بالترتيب)
| # | الخطوة | يغلق |
|---|---|---|
| 1 | نقل الكود المعتمد (الشريحة 1: السيرفر واللوحة والاختبارات) والملفات الحاكمة إلى `wasm-media/wasm-app` من جلسة Claude Code المربوطة، على فرع وPR. **تبقى خارج المستودع العام:** STATE وMEASUREMENT-RULES و01 وBACKLOG وDEFERRED | — |
| 2 | **0أ، الأقفال:** CI (اختبارات SQL وتزامن ولوحة في GitHub Actions)، وحارس `guard` بنسخة main، وCODEOWNERS = عيسى، وحماية main (لا دفع مباشر، الفحوص إلزامية، موافقة عيسى تسقط مع أي commit جديد)، وبيئة `production` مقيّدة بـmain. إعدادات GitHub التي تحتاج صلاحية مدير يضبطها Claude من كروم عيسى | P2 |
| 3 | اختبارات الشريحة 1 في CI الحقيقي: أحمر قبل البناء ثم أخضر، مع اختبار اللوحة ضد مشروع Supabase التطوير الحقيقي | P3، P20 |
| 4 | **0ب، القاعدة والنسخ:** مشروع إنتاج في حساب Supabase الثاني. الترحيلات يطبّقها CI من main فقط. نسخة يومية مشفّرة إلى مستودع نسخ خاص، واستعادة تجريبية في التطوير. Claude لا يرى مشروع الإنتاج ولا أسراره (I12) | P6، P7، P13 |
| 5 | إعدادات Auth في المشروعين: إغلاق التسجيل الذاتي، و«كشف إعادة استعمال رموز التجديد» مفعّل ومقروء. يضبطها Claude من كروم عيسى، ويقيسها | P17، P19 |
| 6 | ترحيل زرع الشريكين: عيسى وأحمد في الأدوار والأسماء، بحسابين تُرسل لهما دعوات من Supabase، وكل واحد يضع كلمة سره بنفسه | P18 |
| 7 | نشر اللوحة على GitHub Pages من main آليًا، بعنوان المشروع ومفتاحه العام. هذا مجاني، والنطاق الخاص لاحقًا | — |
| 8 | تجميد التطبيق القديم: أرشفة `hisabat-isa/wasm-media-accounting` من كروم عيسى، للقراءة فقط | P9 |
| 9 | حارس المؤجلات (§9.3) على محطة «أول بيانات حقيقية»، ثم التسليم | — |
| 10 | عيسى وأحمد يجرّبان ويفتحان أول شغلة حقيقية | P5، P16 |

**خارج الحزمة:** كل ما يخص المراحل اللاحقة (دفترة، والمهام، والموظفون، وعروض الأسعار)، وتحويل المستودع إلى خاص (P10)، وبنود BACKLOG.

## ما يعمله عيسى بيده فقط (لا يُفوَّض أبدًا)
| # | متى | الشغلة | الوقت |
|---|---|---|---|
| H1 | في البداية، مع الموافقة | إنشاء حساب Supabase ثانٍ ببريد غير `isa.des992@gmail.com`، وإنشاء مشروع إنتاج فيه (المنطقة eu-central-1). كلمة سر القاعدة يكتبها بنفسه في أسرار GitHub، في صفحة يفتحها له Claude | حوالي 10 دقائق |
| H2 | في النهاية | ضغطتا Approve: الأولى على PR الأقفال، ثم على PR الشريحة 1 والقاعدة | دقيقتان |
| H3 | في النهاية | فتح دعوة Supabase ووضع كلمة السر، وكذلك أحمد | دقيقتان لكل واحد |

## شروط التوقف (فقط)
1. خطوة من H1 إلى H3 لم تُعمل.
2. عيب أمني لا يُصلح داخل نطاق الحزمة.
3. إجراء لا رجعة فيه غير مذكور هنا.
4. دفع مالي من أي نوع. **الحزمة كلها مجانية:** GitHub Free، وSupabase Free بمشروعين، وGitHub Pages.

## التسليم في النهاية
رسالة واحدة فيها:
- رابط التطبيق الحي.
- رابطا الـPR للضغط.
- حكم المراجع لكل قطعة.
- حكم حارس المؤجلات.
- ما بقي مفتوحًا ولماذا.
