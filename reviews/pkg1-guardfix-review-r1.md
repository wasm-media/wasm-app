reviewed: commit a39337b07088

# مراجعة مستقلة: حزمة الإصلاح «حارس المسارات» داخل الحزمة 1، الجولة 1 من 2

- **الـcommit المراجَع:** `a39337b07088e276395b6fb0aab0874c896c5f1c`. هو رأس الـPR wasm-media/wasm-app#1 على GitHub وقت المراجعة:
  - `pull_request_read` أعاد `head.sha` نفسه.
  - `base` = `1ac40db`.
  - `main` على GitHub فيه commit واحد (`Initial commit`)، و`protected:false`.
- **الفرق المفحوص:** `git diff 9bcb70abca6f a39337b07088`. يمسّ 4 ملفات: guard.sh، وguard_selftest.sh، وci.yml، وbackup.yml.
- **بيئة العمل:**
  - نسخة نظيفة من الـcommit عبر `git archive` في `/tmp/rg`.
  - نسخة فيها `guard.sh` من 9bcb70a في `/tmp/rgold`.
  - هجمات في مستودعات git مؤقتة تحت `/tmp/ra`.
- **ما لم ألمسه:**
  - لم أعدّل شيئًا في `/home/user/wasm-app`: الشجرة نظيفة، والرأس `a39337b`.
  - لا commit ولا push، ولا لمس للإنتاج ولا لأي سر.
- **حادث أثناء العمل:** نسخت سكربت الفحص الذاتي خارج الشجرة، فصار `ROOT=/` وبدأ ينسخ نظام الملفات إلى `/tmp/tmp.*`. أوقفته خلال ثوانٍ وحذفت النسخة الجزئية. لم يُكتب شيء خارج `/tmp`.

---

## 1) الاختبارات: ما شغّلته بنفسي

| الأمر (من `/tmp/rg` على a39337b) | المخرج |
|---|---|
| `bash tests/ci/guard_selftest.sh` | 21 من 21 `ok`، ثم `PASS guard selftest (21)`، ورمز الخروج 0. السجل في `/tmp/review/g3_guard_selftest.log` |
| نسخة من الفحص الذاتي تطبع سبب الرفض (داخل نسخة من الشجرة) | كل فخ من t15 إلى t21 يُرفض **للسبب المقصود** لا لسبب آخر: `guard files touched: …` للبروتوكول، وINVARIANTS، و`.github/CODEOWNERS CODEOWNERS`، و`de\"ploy.yml`، و`$'tests/ci/a\tb.sh'`، و`docs/CODEOWNERS`. أما t21 فيُرفض بـ`test files deleted: tests/sql/concurrency.sh` |
| `/tmp/actionlint .github/workflows/*.yml` | لا مخرج، ورمز الخروج 0 |
| `supabase stop --no-backup` (من `/tmp/wr`) ثم `supabase start -x realtime,storage-api,imgproxy,studio,edge-runtime,logflare,vector` من `/tmp/rg` (CLI 2.118.0) | طُبّقت الترحيلات الثلاث على قاعدة جديدة |
| `bash tests/ci/run_real.sh` | رمز الخروج 0، و106 أسطر `ok`. السجل في `/tmp/review/g3_run_real.log`. أهم ما فيه:<br>`sql: WASM_TEST_RESULTS 152 total, 152 ok, FAILED: none`<br>`PASS concurrency (3 checks)`<br>`PASS [390x844/ar-PS] 53 checks` · `PASS [1440x900/en-US] 53 checks`<br>`PASS real` |
| `bash tests/ci/run_mock.sh` (المنفذ 8080 كان فارغًا قبله وبعده) | `PASS [390x844/ar-PS] 45 checks` · `PASS [1440x900/en-US] 45 checks` · `PASS mock`، ورمز الخروج 0. السجل في `/tmp/review/g3_run_mock.log` |

**CI على GitHub** (قرأت السجلات بنفسي): التشغيل 36183253320 على a39337b أخضر في مهامه الثلاث.
- **guard-selftest:** السجل فيه الفخاخ الـ21 كلها `ok`، ثم `PASS guard selftest (21)`، وgit على الـrunner 2.55.0.
- **ui-mock:** أخضر.
- **supabase-real:** أخضر.

## 2) كسر «لا يستطيع» بهويات حقيقية

المكدّس الحقيقي، والهويات من GoTrue: `employee` و`partner1` من تشغيل `run_real.sh`، وطلب ثالث بلا دخول. هذا الـcommit لا يمسّ القاعدة ولا الواجهة، فالمطلوب هنا التأكد من أن شيئًا لم يرجع.

| المحاولة | بلا دخول | موظف | شريك |
|---|---|---|---|
| قراءة `jobs` | 401 42501 | 200 `[]` | 200 (بيانات) |
| إدراج في `user_roles` | 401 | 403 42501 | 403 42501 |
| `DELETE jobs` | 401 | 403 42501 | 403 42501 |
| `PATCH jobs` (تعديل مباشر) | 401 | 403 | 403 |
| `rpc/create_job` | 401 42501 | 400 `not_partner` | 200 (9003) |
| `rpc/move_job` و`rpc/cancel_job` | 401 42501 | 400 `not_partner` | يصل للدالة |
| `/auth/v1/signup` | 422 `signup_disabled` | — | — |

بعد المحاولات كلها بقيت الشغلة 9001 موجودة، وعنوانها لم يتغيّر. السيرفر رفض كل محاولة.

## 3) الأحمر قبل البناء

- **لا يوجد تشغيل CI أحمر للفخاخ السبعة الجديدة.** أُضيفت في الـcommit نفسه الذي يصلح الحارس، فهي مخالفة إجرائية لـ§2 خطوة 2، كما في الجولة السابقة.
- **أعدت إنتاج الأحمر محليًا:** شغّلت `guard_selftest.sh` الجديد مع `guard.sh` من 9bcb70a. ظهرت الفخاخ t15 إلى t21 كلها `BAD … → PASS (want FAIL)`، ثم `FAIL guard selftest`، ورمز الخروج 1. فالفخاخ ترى شيئًا فعلًا.

## 4) الصنف كله: هل بقي طريق يغيّر ملف حراسة، أو يحذف ملف اختبار أو ينقله، أو يمرّر تقريرًا غير معتمد، والحارس يخرج PASS؟

الحارس هنا من الـcommit نفسه (`/tmp/rg/.github/guard/guard.sh`). كل حالة فيها تقرير اعتماد صحيح على آخر commit، ما لم يُذكر غير ذلك.

### ما أُغلق فعلًا: كلها FAIL كما يجب

| الحالة | الناتج |
|---|---|
| تغيير النمط (mode) لـ`tests/ci/run_real.sh`، ولـ`docs/INVARIANTS.md` | `guard files touched: …`، ورمز الخروج 1 |
| `guard.sh` يصير symlink إلى `/dev/null`، و`INVARIANTS.md` يصير symlink إلى README | FAIL |
| `.github/` كله يُنقل إلى `gh2/` ويوضع مكانه symlink | FAIL، ويسرد كل ملفات `.github/` |
| gitlink (submodule) تحت `.github/workflows/sub`، وفي `CODEOWNERS` بالجذر، وبدل مجلد `tests/ci` | FAIL |
| سطر جديد في اسم تحت `tests/ci/`، أو CR تحت `.github/`، أو `"` و`\` و`$(id)`، أو Unicode (é مع U+202E) | FAIL، والأسماء مطبوعة بـ`%q` فلا تنفذ أي أمر |
| نقل ملف اختبار إلى خارج `tests/`، أو تغيير حالة أحرفه (`Concurrency.sh`)، أو `tests/COUNT` إلى `tests/count` | FAIL (`test files deleted` و/أو `guard files touched`) |
| commit دمج «شرير»: حلّ الدمج نفسه يعدّل `guard.sh` | FAIL |
| تاريخ غير مرتبط (orphan) | `cannot diff`، ورمز الخروج 1 |
| **التقارير:** CRLF، وsymlink، وgitlink، و`Reviews/` بحرف كبير، ومحرف صفري العرض داخل السطر، وNBSP بعد السطر، وBLOCKED فوقه سطر الاعتماد في سطر غير أخير | كلها FAIL. أي أن الحارس يسقط إلى الرفض |
| دمج بين فرع مراجَع وفرع غير مراجَع بتاريخ قديم، والتقرير على commit الفرع المراجَع | FAIL (`LAST` = commit الدمج) |
| PR = دمج مع commit قديم من main بشجرته (يرجع الكود بلا تقرير) | FAIL: `LAST` = commit الدمج، ولا تقرير له |
| **الطلب السليم** (سطر في `web/app.js` + تقرير) | PASS كما يجب |

ما مرّ ولا ضرر فيه:
- نسخ ملف محمي إلى اسم جديد، ثم تعديل النسخة.
- `.GitHub/workflows/x.yml` و`codeowners` و`Docs/INVARIANTS.md`: GitHub لا يقرأ هذه الأسماء، و`.github/CODEOWNERS` المحمي له الأولوية.
- `.gitmodules` وحده.
- ملف اسمه `x⏎.github/w.yml`.
- تقارير سليمة بأسماء فيها سطر جديد أو علامات اقتباس.

### ما لم يُغلق: المانع أدناه، ومعه حالتان في BACKLOG

---

## المانع (BLOCKED_FOR_CORRECTION)

### المانع 1: الحارس يقارن بقاعدة دمج واحدة يختارها git، لا بما سيدمجه GitHub فعلًا. عند تعدّد قواعد الدمج يمرّ PR يرجع ملف حراسة على main، وخروج الحارس PASS
هذا تجاوز للقفل 3 ولقبول 0أ بند 2.6: «يلمس ملفات الحراسة … فئة بفئة». وهو من الصنف المطلوب إغلاقه صراحة في هذه الحزمة («commits دمج، base قديم»).

- **الموضع:** `.github/guard/guard.sh`:
  - الأسطر 11 و12 و25: `paths()` وحذف ملفات الاختبار عبر `git diff … "$BASE...$HEAD"`.
  - السطر 26 (فحص التخطي) والسطر 44 (الأسرار) بالصيغة نفسها.
  - ثلاث النقاط (`...`) تقارن HEAD بقاعدة دمج **واحدة**. إن كان لـBASE وHEAD أكثر من قاعدة دمج، يختار git الأحدث تاريخًا ويطبع تحذيرًا على stderr، ويتجاهله الحارس. أما دمج GitHub (ort) فيبني قاعدة افتراضية من القواعد كلها.
- **الشرط المسبق على main:** دُمج فيه (بـcommit دمج) PR لم يكن محدَّثًا، بعد تعديل عيسى لملف حراسة مباشرة على main. هذا هو المسار الذي يصفه الحارس نفسه: «عيسى يعدّلها بنفسه على main». ويكفي أن يكون تاريخ commit ذلك الـPR بعد تعديل عيسى، والمنفّذ يتحكم بتاريخ commits فروعه.
  - حماية main الآن `protected:false`، وخيار «Require branches to be up to date» غير مقيس.
  - main اليوم commit واحد، فالثغرة كامنة لا مستغَلة. لكنها تُفتح بأول تعديل حراسة من عيسى يتزامن مع PR مفتوح.
- **خطوات الإعادة:** السكربت محفوظ في `/tmp/ra/b1.sh`، ويعمل على نسخة `/tmp/rg`. التشغيل: `bash /tmp/ra/b1.sh /tmp/rg/.github/guard/guard.sh`. ما يفعله:
  ```
  M0 = base
  p1  (فرع PR من M0): سطر في web/app.js، تاريخ commit 12:00
  main: عيسى يضيف قاعدة إلى .github/guard/guard.sh ← M1 (10:00)؛ ثم «Merge PR p1» بلا تحديث ← M2
  PR المهاجم من P1: git merge --no-commit M1 ثم git checkout P1 -- .github/guard/guard.sh (إبقاء النسخة القديمة)
                    ثم سطر كود + تقرير اعتماد على آخر commit كود
  bash guard.sh M2 HEAD ؛ ثم git merge atk في main
  ```
- **المتوقع:** `::error::guard files touched: .github/guard/guard.sh` ثم `guard: FAIL`، ورمز الخروج 1. فالدمج سيُرجع `guard.sh` على main إلى ما قبل تعديل عيسى.
- **الفعلي:**
  ```
  merge bases: 56068da 2c66fca  (P1=56068da M1=2c66fca)
  diff the guard sees for guard.sh: []
  guard: PASS
  guard rc=0
  after merge on main, Isa's rule lines: 0
  ```
  أي أن الحارس خرج بـPASS، والدمج الحقيقي حذف قاعدة عيسى من `guard.sh`.
- **الأثر على I10:** المتغيّر `/tmp/ra/b1_count.sh` يعيد الخطوات نفسها على `tests/COUNT`: عيسى يرفع `sql 152` إلى `sql 160`، والمهاجم يُبقي النسخة القديمة. الناتج `guard: PASS`، وبعد الدمج `sql 152`. فعدد الاختبارات ينقص عبر PR والحارس أخضر، وهذا خرق لـI10.
- **ملاحظات:**
  - الثغرة نفسها موجودة في 9bcb70a: الأمر نفسه على `/tmp/rgold` يعطي PASS. فهي ليست رجوعًا، لكنها من الصنف الذي فُصلت هذه الحزمة لإغلاقه كله.
  - الفخاخ لا تغطي الدمج ولا تعدّد قواعد الدمج.
  - للعلم فقط، دون إصلاح: رفض أي تعدّد في `git merge-base --all "$BASE" "$HEAD"`، أو مقارنة BASE بنتيجة `git merge-tree --write-tree "$BASE" "$HEAD"`. كلاهما يُمسك الحالة، وجرّبت الأول يدويًا بالنظر إلى المخرج أعلاه.
  - **متغيّر معلَّق على سلوك GitHub** (غير مقيس، يحتاج دفعًا): إن كان `pull_request.base.sha` في الحدث أقدم من رأس main، يمرّ PR يدمج main ثم يرجع ملف حراسة. قسته محليًا:
    - `BASE=M0` قديم: `guard: PASS`.
    - `BASE=M1` الحالي: FAIL.
    - وأغلب ظني أن حدث `synchronize` يحدّث `base.sha`، فلم أبنِ عليه الرفض.

---

## 5) الرجوع

- لا رجوع:
  - فخاخ الفحص الذاتي الأربعة عشر السابقة كلها ما زالت FAIL، والطلب السليم t11 ما زال PASS، ومحليًا وفي CI.
  - SQL 152، والتزامن 3، والحقيقي 53×2، والمحاكي 45×2.
  - الرفض بالهويات كما في §2.
- `ci.yml`: تغيّر اسم الخطوة فقط («20 trap PRs and pass 1 clean PR»). والعدد صحيح: 21 طلبًا، منها 20 فخًا وطلب سليم واحد.

## 6) الأمن في التغييرات الجديدة

### `guard.sh`
- المسارات تُقرأ بـ`-z`، و`--no-renames`، و`mapfile -d ''`.
- المطابقة بـ`[[ =~ ]]` على الاسم الخام، والطباعة بـ`printf %q`.
- لا `eval`، ولا تنفيذ لأي شيء من الـPR.
- فشل `git diff` يخرج 1 (`cannot diff`).
- `guard.yml` لم يتغيّر: ما زال يفحص نسخة main (`base.sha`)، ويجلب رأس الـPR كبيانات فقط، و`persist-credentials: false`، و`contents: read`، ولا أسرار. فالحارس ما زال لا يشغّل كود الـPR.

### `backup.yml`: فصل الرفع عن الاستعادة
شغّلت نصوص الخطوات من 4 إلى 8 مستخرَجة من الـYAML، في `/tmp/ra/bk/drive.sh`:
- **بيئة التشغيل:** `bash -e` كما يشغّلها GitHub (سجل الـCI يُظهر `shell: /usr/bin/bash -e {0}`، و`backup.yml` بلا `defaults.run.shell`). وقلّدت شرطَي `if:`.
- **المدخلات:** مستودع bare بدل GitHub (`REMOTE`)، ونسخ حقيقية من المكدّس المحلي مشفّرة بالخطوة 4.
- **مفتاح النشر:** `BACKUP_DEPLOY_KEY` صار في خطوتَي الرفع وحدهما. لم يعد في بيئة `supabase start` وpsql على بيانات الإنتاج. هذا تحسّن أمني يغلق BACKLOG 3 من الجولة السابقة.
- **النسخة سليمة:** الاستعادة `rc=0`، ثم رُفع التقرير المشفّر، ثم `PASS restore test`. المستودع فيه 4 ملفات. رمز خروج كل خطوة 0.
- **النسخة فاسدة** (صف مكرر في `public.jobs`): الخطوة 6 خرجت 1، فتخطّى GitHub خطوة رفع التقرير، ثم طبعت خطوة الحكم `FAIL restore test (rc=none)` وخرجت 1. **فالمهمة تستطيع الفشل.** والنسخ الثلاث رُفعت قبل ذلك.
- **فشل رفع التقرير بعد استعادة سليمة:** يحمّر المهمة، لأن الخطوة بـ`set -euo pipefail` ولأن `push_private.sh` يخرج 1. هذا يغلق BACKLOG 2 من الجولة السابقة.
- **لا تسرّب:** لا قيمة من البيانات ولا عدد في السجل العام. وخطوة الاستعادة لا تطبع إلا `FAIL: restore` عبر `QUIET=1`.
- **لكن عند فشل الاستعادة على GitHub لا يُرفع أي تقرير مشفّر،** خلافًا لرسالة الـcommit («مجرَّب محليًا: rc=0 و rc=1 مع تقرير مشفّر في الحالتين»). السبب في BACKLOG 1. ليس مانعًا:
  - لا فقدان بيانات، فالنسخ مرفوعة أولًا.
  - لا ثغرة، والمهمة حمراء.
  - ليس رجوعًا، فالسلوك نفسه في 9bcb70a.

## 7) الثوابت

| # | الحالة | الدليل |
|---|---|---|
| I1 | لا مساس | الموظف يقرأ `[]` |
| I2، I3، I7 | لا مساس | خارج الفرق؛ الموظف يُرفض بـ`not_partner` |
| I4 | سليم | فحوص I4 ضمن الـ152 خضراء |
| I5 | سليم | بلا دخول: 401 على كل شيء |
| I6 | لا مساس | `public_key.sh` لم يتغيّر |
| I8 | سليم | DELETE مرفوض من كل دور |
| I9 | سليم | فحوص السجل والتزامن خضراء |
| **I10** | **مخروق بطريق المانع 1** | ملف العدّ `tests/COUNT` يرجع إلى رقم أقل عبر PR، والحارس PASS (`/tmp/ra/b1_count.sh`). طرق النقل والحذف المباشرة أُغلقت |
| I11 | لا مساس | — |
| I12 | غير مفروض آليًا على جلسة المنفّذ (N13) | قرار عيسى قبل الدمج، وغير مقيس هنا |
| I13 | سليم | فحوص الخروج خضراء في المحاكي والحقيقي |

---

## BACKLOG (لا توقف بها)

1. **`backup.yml`، خطوة الاستعادة:** `set -uo pipefail` لا يلغي `-e` الذي يضيفه GitHub (`bash -e {0}`).
   - فعند فشل `restore_check.sh` في السطر 79 تتوقف الخطوة فورًا، قبل `rc=$?`. فلا تقرير مشفّر يُرفع، ويبقى `data.sql` المفكوك و`counts.txt` على قرص الـrunner المؤقت حتى نهاية المهمة.
   - ورسالة الحكم تقول «details in the encrypted report» وهو غير موجود.
   - قسته: `bash -e` ثم `rc=none` وبلا تقرير، مقابل `bash` بلا `-e` حيث `rc=1` والتقرير مرفوع.
   - ما زالت المهمة تحمرّ.
   - وكذلك السطر 78: `gpg | gunzip` تحت `-e` و`pipefail` يوقف الخطوة عند فشل فك التشفير.
2. **`backup.yml` السطر 94:** `${{ steps.restore.outputs.rc }}` مُدرج مباشرة في نص `run`. قيمته رقمية من الخطوة نفسها، لكن الأسلم تمريره عبر `env` كما في خطوة الحكم.
3. **الحذف بتغيير النوع:** ملف اختبار يصير symlink (`tests/sql/concurrency.sh` إلى `/dev/null`) أو gitlink (`tests/sql/slice1a_test.sql`)، فيظهر `T` لا `D`، ويخرج الحارس PASS.
   - أثره في CI أحمر: `run_real.sh` لا يجد `WASM_TEST_RESULTS`، أو عدد التزامن ينقص.
   - فالطلب يبقى غير قابل للدمج، ويبقى من صنف «تعديل ملف الاختبار» الذي دفاعه المراجع (BACKLOG 1 في الجولة السابقة).
   - لكن رسالة الحارس «لا حذف لملف اختبار» لا تشمله. `--diff-filter=DT` يُمسكه.
4. **سباق TOCTOU في `guard.yml` (غير مقيس، يحتاج دفعًا):** الحارس يجلب `refs/pull/N/head` وقت التشغيل، لا `pull_request.head.sha` الخاص بالحدث. فلو تغيّر الرأس بين الحدث والجلب، قد يُقيَّم commit غير الذي تُعلَّق عليه النتيجة. الأسلم جلب الـSHA الخاص بالحدث بعينه، والتحقق منه.
5. **مطابقة أول سطر في التقرير** على أول 12 محرفًا فقط، دون حدّ نهاية: `reviewed: commit <12>ffffffff` يُقبل. احتمال التصادم ضئيل.
6. **سجل الحارس يطبع الأسماء العربية بصيغة `$'docs/00-\330…'`** (بسبب `%q`)، فيصعب على عيسى قراءتها. الأمان مصون، والأثر على الوضوح فقط.
7. **لا تشغيل CI أحمر للفخاخ السبعة الجديدة.** أثبتُّ احمرارها محليًا (§3).
8. **كشف التخطي والأسرار نصّي** (`grep`)، ويمرّ بصيغ بديلة مثل `test['skip']`، وكذلك ملف ثنائي فيه NUL. هذا بنيوي، كما في الجولات السابقة. وفحص الأسرار ما زال لا يلتقط JWT من نوع service_role.
9. **ملفات تحت `reviews/`** (مثل `reviews/x.js`) لا تُحسب كودًا في `LAST`. لا شيء ينفّذها اليوم.
10. **بنود BACKLOG السابقة التي لم يمسّها الفرق ما زالت قائمة:**
    - `DECISIONS.md` و`MISTAKES.md` غير محميين.
    - الـactions مثبّتة بالوسم لا بالـSHA.
    - الفرع `probe/p1-write-check` باقٍ.
    - بنود `invites.sh`.
    - N13.
    - ترحيل بصمتَي الشريكين.

## ما لم أستطع قياسه

- **إعدادات GitHub:** حماية main، وخيار «up to date»، ونوع الدمج المسموح، ومصدر الفحص المطلوب. الموجود اليوم أن `main` بلا حماية.
- **دلالة `pull_request.base.sha`** في حدث `synchronize` على `pull_request_target`.
- **تشغيل `backup.yml` الفعلي على الإنتاج** (ممنوع). شغّلت نصوص خطواته محليًا بصَدَفة `bash -e` بديلًا.

الحكم: BLOCKED_FOR_CORRECTION
